<%= form_tag(bills_path), :method => "get" do %>
	<div class = "input-append">
		<% text_field_tag :search, params:[search], placeholder: "Informe o número da NF ou texto" %>
		<button class="btn" type="submit">Pesquisar</button>
	</div>
<% end %>

<% if params[:search] %>
	<%= @bills.each do |bill| %>
		<%= bill.numero_documento %>
	<% end %>
<% end %>

